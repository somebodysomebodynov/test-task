📈 Number Sorter Microservice

📝 Описание Проекта
Этот микросервис на Spring Boot предназначен для приема одного числового значения,
сохранения его в базе данных PostgreSQL и возвращения клиенту отсортированного списка всех чисел, 
когда-либо полученных сервисом.

🎯 Основные Задачи:
 - Прием числа через REST API.
 - Сохранение числа в PostgreSQL (с использованием Hibernate/JPA).
 - Извлечение всех сохраненных чисел.
 - Сортировка и возврат полного списка чисел в ответе.
   
Пример Работы:
   Входной Запрос | Состояние БД | Ответ API
       3          |       [3]    |     [3]
       2          |   [3, 2]     |    [2, 3]
       1          |  [3, 2, 1]   |  [1, 2, 3]

⚙️ Запуск Проекта (Docker Compose)
Проект настроен для запуска одной командой с помощью Docker Compose -> развернет микросервис и базу данных PostgreSQL.

💻Используйте следующую команду для сборки Docker-образа и запуска всех сервисов:

docker compose up --build -d

Сервис будет доступен на порту 8080

💻Запрос добавление числа: 

curl -X POST http://localhost:8080/api/numbers \
-H "Content-Type: application/json" \
-d '{"value": 3.14}'

Ожидаемый формат ответа (JSON):
{
  "numbers": [
   3.14
  ]
}
